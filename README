NOTE: To install in CF Builder, you'll need to extract the GitHub-generated .zip file and use the "install extension from folder" option in Builder.

A ColdFusion Builder 2 extension that displays the default Builder shortcut keys in an Eclipse view.